import{Link}from'react-router-dom';import{courses,activeProgram}from'../data/demo';export default function Courses(){return <div className="container"><span className="eyebrow">ACTIVE PROGRAM</span><h1>{activeProgram.department}</h1><p>{activeProgram.level} • {activeProgram.semester}</p><div className="course-grid">{courses.map(c=><Link to="/topic" className="card coursebox" key={c.code}><span>{c.code}</span><h2>{c.name}</h2><p>Active learning content</p><div className="bar"><i style={{width:c.progress+'%'}}/></div></Link>)}</div><div className="coming card"><h2>More departments & levels</h2><p>Other programmes and levels will appear as Coming Soon until their content is prepared.</p></div></div>}