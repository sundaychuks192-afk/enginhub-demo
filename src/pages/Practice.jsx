import{useState}from'react';import{questions}from'../data/demo';export default function Practice(){const[selected,setSelected]=useState({});const[done,setDone]=useState(false);const score=questions.reduce((s,q,i)=>s+(selected[i]===q.answer?1:0),0);return <div className="container"><span className="eyebrow">MEC 124 • PRACTICE</span><h1>Today's Engineering Challenge</h1><p>Test yourself with demo questions. Larger school question banks will be connected to Supabase later.</p>{questions.map((q,i)=><div className="card question" key={q.q}><h2>{i+1}. {q.q}</h2>{q.options.map((o,j)=><button className={selected[i]===j?'option selected':'option'} onClick={()=>setSelected({...selected,[i]:j})} key={o}>{o}</button>)}</div>)}<button className="primary button" onClick={()=>setDone(true)}>Submit challenge</button>{done&&<div className="result card"><h2>Score: {score}/{questions.length}</h2><p>{score===questions.length?'Excellent. Keep your streak going!':'Review the topic and try again.'}</p></div>}</div>}