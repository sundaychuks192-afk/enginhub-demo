export function LecturerDashboard(){return <div className="container-wide page-pad"><span className="eyebrow">LECTURER WORKSPACE</span><h1>Teach with <em>context.</em></h1><p className="lead">Manage courses, lessons, assignments and student progress from one place.</p><div className="feature-grid">{['Courses & topics','Upload materials','Assignments','Student progress'].map(x=><div className="card" key={x}><span className="tag">LECTURER</span><h3>{x}</h3><p>Production-ready area for the content backend.</p></div>)}</div></div>}
export function AdminDashboard(){return <div className="container-wide page-pad"><span className="eyebrow">ADMIN CONTROL</span><h1>Run the <em>ecosystem.</em></h1><p className="lead">Manage users, institutions, departments, courses, question banks, content and platform analytics.</p><div className="feature-grid">{['Users & roles','Institutions','Course library','Platform analytics'].map(x=><div className="card" key={x}><span className="tag">ADMIN</span><h3>{x}</h3><p>Production-ready area for secure backend permissions.</p></div>)}</div></div>}
