const lecturer=['Courses & topics','Upload materials','Assignments','Student progress'];
const admin=['Users & roles','Institutions','Course library','Question bank','Platform analytics','Content quality'];
export function LecturerDashboard(){return <div className="container-wide page-pad"><section className="role-hero"><div><span className="eyebrow">LECTURER WORKSPACE</span><h1>Teach with <em>context.</em></h1><p className="lead">A visual workspace for building course content, publishing notes, creating assignments and seeing where students need help.</p></div><div className="role-art lecturer-art"><span>ENGIHUB</span><b>COURSE<br/>STUDIO</b></div></section><div className="feature-grid role-grid">{lecturer.map((x,i)=><div className="card role-card" key={x}><span className="role-number">0{i+1}</span><span className="tag">LECTURER</span><h3>{x}</h3><p>{['Organize topics and learning paths.','Publish notes and visual resources.','Create practice and assessment sets.','Track progress and weak spots.'][i]}</p><button className="secondary" onClick={()=>alert(`${x} workspace is being connected to the EngiHub content backend.`)}>Open workspace →</button></div>)}</div></div>}
export function AdminDashboard(){return <div className="container-wide page-pad"><section className="role-hero"><div><span className="eyebrow">ADMIN CONTROL CENTRE</span><h1>Run the <em>ecosystem.</em></h1><p className="lead">Manage the academic structure, users, content quality and platform health from one visual control centre.</p></div><div className="role-art admin-art"><span>ENGIHUB</span><b>CONTROL<br/>CENTRE</b></div></section><div className="feature-grid role-grid">{admin.map((x,i)=><div className="card role-card" key={x}><span className="role-number">0{i+1}</span><span className="tag">ADMIN</span><h3>{x}</h3><p>Secure management surface for the production platform.</p><button className="secondary" onClick={()=>alert(`${x} management is part of the production administration layer.`)}>Open workspace →</button></div>)}</div></div>}
